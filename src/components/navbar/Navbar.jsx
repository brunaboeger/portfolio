export default function Navbar() {
  return (
    <nav className="navbar">
      <div className="navbar-container">
        <section className="navbar-logo-box">Bruna Boeger</section>
        <section className="navbar-actions-box">
          <button>Tema</button>
          <button>Contato</button>
        </section>
      </div>
    </nav>
  );
}
